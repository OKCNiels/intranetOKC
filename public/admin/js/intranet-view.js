class IntranetView {

    constructor(model) {
        this.model = model;
    }

    /**
     * crea el menu con sus modulos
     */
    renderizarModulos = () => {
        let html = '';
        // this.model.obtenerModulos().then((respuesta) => {
        //     console.log(respuesta);
        // }).fail((respuesta) => {
        //     console.log('error');
        // }).always(() => {
        // });
    }
}



